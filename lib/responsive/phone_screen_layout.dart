import 'package:flutter/material.dart';

class PhoneScreenLayout extends StatelessWidget {
  const PhoneScreenLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('This is phone screen'),
      ),
    );
  }
}
